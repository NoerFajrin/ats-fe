import { css } from "@emotion/css";

export default {
    centeredSpace: css({
        display:'flex',
        justifyContent:'center',
        alignItems:'center',
        minHeight:'100vh'
      })
}