import { css } from "@emotion/css";

const style = {
    container: css({
        display:"flex",
        flexDirection:'column',
        justifyContent:'space-between',
        width:'100%',
    })
}

export default style